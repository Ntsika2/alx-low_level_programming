#include "main.h"
/**
 * _isdigit -checks if character digit
 * @c: Variable text
 * Return: Always 0
 */
int _isdigit(int c)
{
	char 'c';

	if (c ≥ '0' && c  ≤ '9')
	{
		return (1);
	}

		return (0);
}
